export const getMessages = (state) => state.messages.messages;

export const getSocket = (state) => state.config.socket;

export const getUser = (state) => state.user;

export const getUsersTyping = (state) => state.messages.usersTyping;

export const getServerState = (state) => state.config.serverState;
